{
  "name": "txner",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "tf-init": "cd ./deploy && terraform init",
    "tf-plan": "cd ./deploy && terraform plan",
    "tf-apply": "cd ./deploy && terraform apply -auto-approve",
    "tf-destroy": "cd ./deploy && terraform destroy",
    "build-lambda": "rm -rf ./deploy/dist &&  tsc",
    "deploy": "npm run build-lambda && npm run tf-apply"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thanover/txner.git"
  },
  "author": "Tom Hanover",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/thanover/txner/issues"
  },
  "homepage": "https://github.com/thanover/txner#readme",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.609.0",
    "@aws-sdk/util-dynamodb": "^3.609.0",
    "@types/aws-lambda": "^8.10.140",
    "@types/node": "^20.14.10",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.5.3",
    "@types/jest": "^29.5.12",
    "csv": "^6.3.9",
    "ts-jest": "^29.2.2"
  },
  "jest": {
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    }
  }
}